# Brief: All dependencies / pip3 installs should be defined in this file.
#        The dockerfile uses this to build the docker image.
# 
# Format: package-name==version

django==4.2.1
requests==2.30.0
RateMyProfessorAPI==1.3.4
channels==4.0.0
